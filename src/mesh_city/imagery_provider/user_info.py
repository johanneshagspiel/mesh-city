class UserInfo:
	def __init__(self,name,api_key,quota,usage,year,month,day,hour,minute,second):
		self.name=name
		self.api_key = api_key
		self.quota = quota
		self.usage = usage
		self.year = year
		self.month = month
		self.day = day
		self.hour = hour
		self.minute = minute
		self.second = second
