default:
    image: archlinux

stages:
    - setup
    - build
    - check
    - test

setup:
    stage: setup
    cache:
        paths:
            - /usr/*
    script:
        - pacman --noconfirm -S python-pipenv

build:
    stage: build
    script:
        - pwd
        - ls -lAh
